#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int i, j, vet[10], num[10], cont[10];

main()

{
	setlocale(LC_ALL, "Portuguese");
	printf ("\n\t N�meros repetidos com vetor");
	printf ("\n\n Informe 10 n�meros inteiros");
	
	for(i=0;i<10;i++) // leitura do vetor usando a constante
    	{
        scanf ("%d", &vet[i]);
   		}
   	for(i=0;i<10;i++)
   		{
   		 vet[i]	= num[i]
		}
        
        
}
